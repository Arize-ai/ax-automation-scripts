# arize_saml_bulk_setup.py dependencies
# Python 3.11+ required

# Arize toolkit — GraphQL transport for SAML operations only.
# Org/space CRUD goes through the REST API (see arize_saml_bulk_setup/orgs_spaces.py).
arize-toolkit>=0.1.0

# GraphQL client used by the toolkit for the three remaining SAML ops
# (getSAMLIdP, createSAMLIdP, updateSAMLIdP).
gql[requests]>=3.0.0,<4.0

# HTTP transport for the REST API calls (orgs, spaces, roles).
requests>=2.28.1,<3.0

# Test-only dependency (used by tests/conftest.py to mock REST endpoints).
responses>=0.24
