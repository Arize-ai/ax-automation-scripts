# arize_saml_bulk_setup.py dependencies
# Python 3.11+ required

# Arize toolkit — org/space management + GQL client foundation
arize-toolkit>=0.1.0

# GraphQL client used directly for SAML + org creation operations
gql[requests]>=3.0.0,<4.0

# HTTP transport (pulled in by gql[requests], pinned here for clarity)
requests>=2.28.1,<3.0
